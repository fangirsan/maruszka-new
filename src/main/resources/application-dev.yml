spring:
  datasource:
#    url: jdbc:mysql://localhost:3306/maruszka_dev?useTimezone=true&serverTimezone=UTC&characterEncoding=utf8
    url: jdbc:postgresql://localhost:5432/maruszka_dev_db
    username: maruszka_dev_user
    password: maruszka
#    platform: mysql
    platform: postgresql
  jpa:
    hibernate:
      ddl-auto: validate
      jdbc:
#    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
#    database: mysql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql
    show-sql: false
    properties:
      hibernate:
        hbm2ddl:
          delimiter: ";"
      javax:
        persistence:
          schema-generation:
            create-source: metadata
            scripts:
              action: create
              create-target: maruszka_database_create.sql